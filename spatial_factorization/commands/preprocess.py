"""Preprocess dataset into standardized format (Stage 1)."""

# STAGE 1: Implementation pending
# This command will standardize data format (run once per dataset)
# Output: X.npy, Y.npy, C.npy, metadata.json in outputs/{dataset}/preprocessed/


def run(config_path: str):
    """Preprocess dataset and save standardized files."""
    print(f"Preprocess command not yet implemented.")
    print(f"Config: {config_path}")
    print("This will be implemented in Stage 1.")
