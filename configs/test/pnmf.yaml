name: test_pnmf
seed: 42
dataset: test
output_dir: outputs/test

preprocessing:
  spatial_scale: 50.0

model:
  E: 3
  loadings_mode: multiplicative
  n_components: 10
  training_mode: standard
  mode: expanded
  spatial: false
  prior: GaussianPrior

training:
  max_iter: 1000
  learning_rate: 0.01
  device: cpu
